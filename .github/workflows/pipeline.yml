name: Login Page Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: [self-hosted, login]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Python file
        run: python3 login.py
